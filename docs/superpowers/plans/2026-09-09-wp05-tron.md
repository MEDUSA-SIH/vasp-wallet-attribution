# WP-05 TRON Provider Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace TronProvider stub with live Tatum-backed provider following WP-03/WP-04 patterns.

**Architecture:** TronProvider(BlockchainProvider) in api/app/providers/tron.py uses Tatum REST for balance + history and Tatum gateway JSON-RPC eth_blockNumber for height. All URLs/keys via Settings, no hardcodes. Factory gates on provider_tron_enabled.

**Tech Stack:** Python 3.12, httpx AsyncClient (10s), pydantic-settings, pytest + MockTransport, ruff, mypy strict.

**Spec:** docs/work-packages.md WP-05 (feature/tron-provider-live, Phase 20); docs/contracts.md §1-2; prior art api/app/providers/bitcoin.py, api/app/providers/ethereum.py.

## Global Constraints

- Python >=3.12, absolute imports from app.xxx import ..., phase docstrings required.
- BlockchainProvider contract frozen: get_balance/get_transactions/stream_transactions/get_block_height/healthcheck, ascending timestamp order, healthcheck <5s, multi-instantiable, no global state.
- CanonicalTransaction frozen: amount human units, fee native TRX, raw JSON-serialisable, slots/frozen.
- No secrets in source — env only via Settings.blockchain_api_key, Settings.tron_provider_url. NEVER commit real key.
- DEMO_MODE=true path untouched; ruff line-length 100; mypy strict=true.
- One WP per branch: feature/tron-provider-live from main; touch only WP-05 files.
- DO NOT COMMIT — leave all changes uncommitted for user review.

---

### Task 1: Failing tests for TronProvider

**Files:**
- Create: `api/tests/unit/test_tron_provider.py`
- Test: `api/tests/unit/test_tron_provider.py`

**Interfaces:**
- Consumes: Settings, BlockchainProvider contract
- Produces: 10 test names Task 2 must satisfy (get_transactions maps, time filter, balance SUN, block height gateway, healthcheck, missing key, 401, stream, rate-limit retry, factory registers live)

- [ ] **Step 1: Write failing test file**

```python
# (full content already specified — see Task 1 brief; 10 tests with mocked Tatum REST + gateway)
```

- [ ] **Step 2: Run test to verify it fails**

Run: `uv run pytest tests/unit/test_tron_provider.py -v`
Expected: FAIL with TypeError / NotImplementedError from stub

- [ ] **Step 3: Leave uncommitted for user review**

Run: `git status --short`
Expected: untracked api/tests/unit/test_tron_provider.py shown, no commit created

### Task 2: Live TronProvider implementation

**Files:**
- Modify: `api/app/providers/tron.py:1-48`
- Test: `api/tests/unit/test_tron_provider.py`

**Interfaces:**
- Consumes: Settings(blockchain_api_key, tron_provider_url), ProviderError, CanonicalTransaction
- Produces: TronProvider(settings=None, client=None) with get_balance/get_transactions/stream_transactions/get_block_height/healthcheck + aclose()

- [ ] **Step 1: Write minimal implementation**

```python
# TronProvider backed by Tatum REST + gateway, SUN->TRX 1e6, TransferContract + TriggerSmartContract best-effort
```

- [ ] **Step 2: Run test to verify it passes**

Run: `uv run pytest tests/unit/test_tron_provider.py -v`
Expected: 10 PASS

- [ ] **Step 3: Leave uncommitted for user review**

Run: `git status --short`
Expected: modified tron.py shown, no commit created

### Task 3: Factory wiring + env docs

**Files:**
- Modify: `api/app/providers/factory.py:61-67`
- Modify: `.env.example:68-76`
- Test: `api/tests/unit/test_tron_provider.py`, `api/tests/unit/test_provider_factory.py`

**Interfaces:**
- Consumes: Settings.provider_tron_enabled
- Produces: live TronProvider(settings) registered in non-demo mode when enabled

- [ ] **Step 1: Wire factory**

```python
elif _code == "tron" and settings.provider_tron_enabled:
    registry.register(TronProvider(settings=settings))
```

- [ ] **Step 2: Document env**

```text
# TRON live path (WP-05): set PROVIDER_TRON_ENABLED=true + BLOCKCHAIN_API_KEY=<Tatum key> + TRON_PROVIDER_URL (default https://api.tatum.io; gateway https://tron-mainnet.gateway.tatum.io/jsonrpc). Never commit real keys.
```

- [ ] **Step 3: Run checks**

Run: `uv run pytest tests/unit/test_tron_provider.py tests/unit/test_provider_factory.py -v`
Run: `uv run ruff check api/ packages/`
Run: `uv run ruff format --check api/ packages/`
Expected: all green, no commit created
